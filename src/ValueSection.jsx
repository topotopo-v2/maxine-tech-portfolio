export default function ValuesSection({ valuesRef }) {
    return (
        <section id="values" ref={valuesRef}>
            <div class="page">
                <div class="page-content">
                    <h1 id="values" class="text-values">Study with me</h1>
                    <div class="summary"><p>I enjoy long periods of focused, independent work, but I’ve also worked in fast-paced environments and understand when communication and meetings are necessary to keep things aligned and moving.</p></div>
                </div>
            </div>
        </section>
    )
}